package planning;

import MyVelib.*;
import java.util.*;
import location.*;

public class ShortestPath extends Policy {
	@Override
	public List<Station> findStations(Coordinates x, Coordinates y, BikeSystem b){
		int minDistance;
		for (Station i:b.getStations()){
			for (Station j:b.getStations()){
				
			}
			
		}
	}

}
